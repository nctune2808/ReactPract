import React from "react";
import { category } from '../apis/tmdb';
const Catalog = () => {
    // const { category } = useParams();
    return(
        <div>
            Catalog
        </div>
    );
}

export default Catalog;