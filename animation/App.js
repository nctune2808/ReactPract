import * as React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

import {HomeScreen, SettingScreen} from './src/screens';

const App = () => {
  return <HomeScreen />;
  //return <SettingScreen />;
};

export default App;
